# 🚀 项目部署指南

## 📋 目录

1. [方案一：Vercel 部署（推荐）](#方案一vercel-部署推荐)
2. [方案二：Netlify 部署](#方案二netlify-部署)
3. [方案三：GitHub Pages 部署](#方案三github-pages-部署)
4. [本地预览构建版本](#本地预览构建版本)

---

## 方案一：Vercel 部署（推荐）⭐

**优点：**
- ✅ 完全免费
- ✅ 速度快，自动 CDN 加速
- ✅ 自动 HTTPS
- ✅ 每次推送代码自动重新部署
- ✅ 配置超简单

### 📝 步骤

#### 1. 准备工作

**1.1 确保代码在 Git 仓库中**

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "准备部署"
```

**1.2 创建 GitHub 仓库**

1. 访问 https://github.com/new
2. 创建新仓库（例如：`vocabulary-app`）
3. **不要**勾选任何初始化选项
4. 点击创建

**1.3 推送代码到 GitHub**

```bash
# 添加远程仓库（替换成你的仓库地址）
git remote add origin https://github.com/你的用户名/vocabulary-app.git

# 推送代码
git branch -M main
git push -u origin main
```

#### 2. 部署到 Vercel

**2.1 注册/登录 Vercel**

1. 访问 https://vercel.com
2. 点击 "Sign Up"
3. 选择 "Continue with GitHub"（用 GitHub 账号登录）

**2.2 导入项目**

1. 登录后，点击 "Add New..." → "Project"
2. 选择 "Import Git Repository"
3. 找到你的 `vocabulary-app` 仓库
4. 点击 "Import"

**2.3 配置项目**

在配置页面：
- **Framework Preset**: 自动检测为 `Vite`
- **Build Command**: `npm run build` （自动填充）
- **Output Directory**: `dist` （自动填充）
- **Install Command**: `npm install` （自动填充）

**直接点击 "Deploy"** ✅

**2.4 等待部署**

- 大约 1-2 分钟
- 部署成功后会显示 🎉 Congratulations!
- 会给你一个网址，类似：`https://vocabulary-app-xxx.vercel.app`

**2.5 访问你的网站**

点击提供的网址，就能看到你的单词学习应用了！🎉

#### 3. 后续更新

以后想更新网站，只需：

```bash
# 修改代码后
git add .
git commit -m "更新功能"
git push

# Vercel 会自动检测并重新部署！✨
```

---

## 方案二：Netlify 部署

**优点：**
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 自动重新部署
- ✅ 提供拖拽部署功能

### 📝 步骤

#### 方法A：通过 Git（推荐）

**1. 准备 GitHub 仓库**

（参考 Vercel 方案的步骤 1）

**2. 部署到 Netlify**

1. 访问 https://www.netlify.com
2. 点击 "Sign Up" → 用 GitHub 登录
3. 点击 "Add new site" → "Import an existing project"
4. 选择 "GitHub"
5. 授权并选择 `vocabulary-app` 仓库
6. 配置：
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
7. 点击 "Deploy site"

**3. 获取网址**

部署成功后，会得到类似 `https://random-name-123.netlify.app` 的网址

**4. 自定义域名（可选）**

在 Netlify 控制台可以修改为更好记的名字，如：
`https://my-vocabulary-app.netlify.app`

#### 方法B：拖拽部署（无需 Git）

**1. 构建项目**

```bash
npm run build
```

会生成 `dist` 文件夹

**2. 拖拽部署**

1. 访问 https://app.netlify.com/drop
2. 直接把 `dist` 文件夹拖进去
3. 等待上传完成

**缺点**: 每次更新都要手动重新拖拽

---

## 方案三：GitHub Pages 部署

**优点：**
- ✅ 完全免费
- ✅ 直接用 GitHub 托管

**缺点：**
- ⚠️ 需要配置 base 路径
- ⚠️ 不会自动部署（需要手动或配置 Actions）

### 📝 步骤

#### 1. 修改配置文件

```bash
# vite.config.ts
```

需要添加 base 路径：

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  base: '/vocabulary-app/', // 替换成你的仓库名
})
```

#### 2. 添加部署脚本

在 `package.json` 中添加：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

#### 3. 安装 gh-pages

```bash
npm install --save-dev gh-pages
```

#### 4. 部署

```bash
npm run deploy
```

#### 5. 配置 GitHub Pages

1. 进入 GitHub 仓库
2. Settings → Pages
3. Source 选择 `gh-pages` 分支
4. 点击 Save

**访问网址**: `https://你的用户名.github.io/vocabulary-app/`

---

## 本地预览构建版本

在部署前，建议先本地测试：

```bash
# 1. 构建
npm run build

# 2. 预览
npm run preview
```

访问显示的本地地址（通常是 `http://localhost:4173`），确认一切正常。

---

## 🎯 推荐方案对比

| 特性 | Vercel | Netlify | GitHub Pages |
|------|--------|---------|--------------|
| 免费 | ✅ | ✅ | ✅ |
| 速度 | ⚡⚡⚡ | ⚡⚡⚡ | ⚡⚡ |
| 自动部署 | ✅ | ✅ | 需配置 |
| HTTPS | ✅ | ✅ | ✅ |
| 自定义域名 | ✅ | ✅ | ✅ |
| 配置难度 | 😊 简单 | 😊 简单 | 🤔 中等 |
| **推荐指数** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

### 我的建议 💡

1. **首选 Vercel** - 最简单，速度快，体验好
2. **次选 Netlify** - 同样优秀，功能类似
3. **GitHub Pages** - 如果只想用 GitHub，且不介意配置

---

## ❓ 常见问题

### Q1: 部署后页面空白？

**检查：**
1. 浏览器控制台是否有错误
2. `vite.config.ts` 中的 `base` 路径是否正确
3. 是否正确构建了 `dist` 文件夹

### Q2: 刷新页面出现 404？

**解决：**
- **Vercel**: 自动处理，无需配置
- **Netlify**: 自动处理，无需配置
- **GitHub Pages**: 需要添加 `404.html` 重定向

### Q3: 数据会丢失吗？

不会！你的单词数据存储在浏览器的 `localStorage` 中：
- 每个用户的数据存在自己的浏览器
- 不同用户看到的是自己的数据
- 清除浏览器数据会丢失，建议定期导出

### Q4: 多个用户可以同时使用吗？

可以！每个访问网站的人都有独立的数据：
- 张三访问：看到张三的单词
- 李四访问：看到李四的单词
- 互不影响 ✅

### Q5: 如何让更多人访问？

部署后，直接分享网址即可：
```
https://vocabulary-app-xxx.vercel.app
```

发给朋友、同学、老师都可以访问！

### Q6: 如何更新已部署的网站？

**Vercel/Netlify（自动）：**
```bash
# 修改代码
git add .
git commit -m "更新"
git push
# 自动部署完成！✨
```

**GitHub Pages（手动）：**
```bash
npm run deploy
```

---

## 🎉 部署成功后

### 可以做的事：

1. **分享给朋友** 📱
   ```
   嘿，我做了个背单词网站，试试看：
   https://your-app.vercel.app
   ```

2. **自定义域名** 🌐
   - 如果你有自己的域名（如 `study.com`）
   - 可以在 Vercel/Netlify 设置中绑定

3. **监控访问** 📊
   - Vercel/Netlify 提供免费的访问统计
   - 可以看到多少人在用

4. **持续改进** 🔧
   - 收集用户反馈
   - 添加新功能
   - 一键部署更新

---

## 🚀 快速开始（最简流程）

### 5分钟部署到 Vercel：

```bash
# 1. 提交代码
git add .
git commit -m "准备部署"

# 2. 推送到 GitHub
# (先在 GitHub 创建仓库)
git remote add origin https://github.com/你的用户名/vocabulary-app.git
git push -u origin main

# 3. 访问 vercel.com 登录
# 4. Import 你的仓库
# 5. 点击 Deploy
# 6. 完成！🎉
```

---

## 📞 需要帮助？

如果遇到问题：

1. **查看构建日志** - Vercel/Netlify 会显示详细错误
2. **检查配置** - 确保 build 命令和输出目录正确
3. **本地测试** - `npm run build && npm run preview`

祝部署顺利！🎊

